{
  "name": "bot-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Self-play arena where a scraping bot competes against bot detection",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "arena:run": "cd packages/arena && pnpm exec tsx src/cli.ts",
    "demo": "cd packages/arena && pnpm exec tsx src/cli.ts --rounds 5 --fast"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
